import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  noContacts: {
    textAlign: "center",
    backgroundColor: "#fff",
    borderRadius: "10px",
    padding: "10px 20px",
    boxShadow: "0px 0px 4px 4px #6e6e6e52",
    margin: "10px",
  },
}));
